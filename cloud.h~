#ifndef CLOUD_H
#define CLOUD_H
#include <QGraphicsPixmapItem> 
#include <QPixmap> 
#include "thing.h" 
class Cloud : public QObject, public Thing {
 	Q_OBJECT
    public:  
 	Cloud(QPixmap*, int, int); 
 	//~Pig(); 
 
    private: 
 	int x_; 
 	int y_; 
    public slots: 
        void move(); 
};  
#endif 

